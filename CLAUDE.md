# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**artipivot** — a Python 3.12 project managed with `pyproject.toml` (currently early-stage / scaffold).

## Commands

- **Run:** `python main.py`
- **Manage dependencies:** `pyproject.toml` using `uv` (project was scaffolded with uv)
